{
  "name": "anna-app-aps-files-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Anna App that stores attachments in APS Files via a bundled Executa (files/* reverse RPC) — no app-level upload grant.",
  "scripts": {
    "dev:aps": "anna-app dev --storage aps",
    "dev:mock": "anna-app dev --mock-llm fixtures/happy-path.jsonl",
    "dev:off": "anna-app dev --no-llm",
    "validate": "anna-app validate"
  },
  "devDependencies": {
    "@anna-ai/cli": "latest"
  }
}
